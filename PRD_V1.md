# 產品需求文件 (PRD)：AI 信用卡消費管家 V1

## 0. 文件資訊
| 項目 | 內容 |
|------|------|
| 文件版本 | V1.0 (As-Built) |
| 建立日期 | 2026年5月18日 |
| 最後更新 | 2026年5月18日 |
| 文件狀態 | 已開發完成 |

---

## 1. 產品概述
本專案為一個**本地端內部工具**，旨在協助使用者管理多張信用卡帳單。透過 PDF 解析、Google Gemini AI 店家名稱清理與規則引擎，自動計算回饋金額並提供消費建議，解決銀行帳單混亂與回饋難以追蹤的痛點。

---

## 2. 核心功能落地方案

### 2.1 PDF 自動解析與安全管理
*   **技術實現**：使用 `pdfplumber` 進行文字提取，搭配銀行特定的 Regex 與 Table 提取邏輯。
*   **密碼保險箱 (Vault)**：採用 `cryptography` 套件的 Fernet (AES-256) 算法，將 PDF 解鎖密碼加密儲存於 SQLite。金鑰儲存於本地 `.env`。
*   **銀行適配**：初步支援 `ctbc` (中信), `taishin` (台新), `esun` (玉山) 等架構，具備可擴展的 Adapter 模式。

### 2.2 Google Gemini AI 消費分類引擎
*   **AI 模型**：使用 **Google Gemini (`gemma-4-31b-it`)**。
*   **店家清理**：將原始交易描述（如 "PX-MART 0123"）清理為標準品牌名（"全聯福利中心"）。
*   **智能分類**：自動歸類為「食、衣、住、行、育、樂、網購、其他」。
*   **網路消費判定**：AI 自動判定交易是否為線上購物，輔助回饋規則匹配。

### 2.3 智慧回饋規則引擎
*   **規則結構**：支援「關鍵字匹配」、「分類匹配」與「線上購物匹配」。
*   **回饋計算**：自動計算每筆交易的預估現金回饋，並支援「單筆/月度回饋上限 (Cap)」邏輯。
*   **最佳卡片建議**：使用者輸入通路關鍵字，系統比對所有持卡規則，給予回饋率最高的卡片建議。

### 2.4 消費分析報表
*   **視覺化圖表**：
    *   **KPI 概覽**：本月總消費、總回饋、平均回饋率。
    *   **類別佔比**：按消費類別 (Pie Chart) 顯示支出結構。
    *   **回饋貢獻度**：按信用卡 (Bar Chart) 顯示回饋貢獻排行。
*   **交易明細**：列出經 AI 清理後的完整交易紀錄。

---

## 3. 落地技術棧 (Tech Stack)

| 層級 | 技術選擇 |
|------|----------|
| 前端框架 | React 19 (Vite) + TypeScript |
| 樣式/UI | Tailwind CSS + Lucide Icons + Recharts + Motion |
| 後端框架 | FastAPI (Python 3.10+) |
| AI 模型 | Google Gemini (`gemma-4-31b-it`) 透過 `google-genai` 庫 |
| 資料庫 | SQLite (非同步 `aiosqlite` + `SQLAlchemy 2.0`) |
| 安全加密 | Cryptography (Fernet AES-256) |

---

## 4. 成功指標達成情況
*   **安全性**：所有金鑰與密碼均經過加密，且資料儲存於本地 SQLite，不外傳雲端。
*   **處理效率**：單份帳單解析與 AI 分類流程在秒級內完成。
*   **易用性**：提供一鍵上傳與即時搜尋建議介面。

---

## 5. 未來擴展建議
*   **多銀行適配**：持續增加國泰、永豐、星展等銀行的 PDF 模板。
*   **截圖 OCR**：增加對刷卡簡訊與 App 推播截圖的辨識功能。
*   **匯出報表**：支援將消費明細匯出為 Excel 或 CSV。
